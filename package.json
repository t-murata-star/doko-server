{
  "name": "doko-restsever",
  "version": "1.0.0",
  "author": "t-murata",
  "description": "行き先掲示板用RESTAPIサーバ",
  "private": true,
  "scripts": {
    "start:mock": "json-server --watch DB.json -m middleware.js --port 3001"
  },
  "dependencies": {
    "json-server": "^0.15.0"
  }
}
